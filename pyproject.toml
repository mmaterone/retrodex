[project]
name = "retrodex"
version = "0.1.0"
description = "Deterministic pixel character cleanup core and TS product shell."
requires-python = ">=3.11"
dependencies = ["pillow>=10.0.0"]

[tool.setuptools]
package-dir = { "" = "python" }

[tool.setuptools.packages.find]
where = ["python"]
